version: 1
principle: "ORION routes -> specialist owners build -> independent verifiers prove"

quality_governance:
  creator_may_final_certify: false
  orion_may_substitute_for_verifier: false
  ui_release_veto_profiles: [prism, lens]
  security_veto_profile: sentinel
  remediation_owner_by_domain:
    design-contract: aurora
    frontend: frame
    backend: forge
    infrastructure: atlas
  retest_owner: original-verifier
  escalation_coordinator: orion
  threshold_override_allowed: false
  stale_evidence_reuse_allowed: false

profiles:
  orion:
    role_class: orchestrator
    model: gpt-terra
    a2a_port: 9901
    production_write: false
    allowed_write_scopes: [mission-ledger, decision-ledger, closure-report, release-decision-record]
    allowed_root_toolsets: [kanban, memory, todo, clarify, session_search, skills]
    allowed_platform_toolsets: [a2a, clarify, hermes-a2a, kanban, memory, session_search, skills, todo]
    forbidden_toolsets: [browser, code_execution, computer_use, cronjob, delegation, file, terminal]

  atlas:
    role_class: production-operator
    model: gpt-terra
    a2a_port: 9902
    production_write: true
    allowed_write_scopes: [infrastructure, deployment, rollback, observability]
    allowed_root_toolsets: [file, terminal, web, kanban, session_search]
    allowed_platform_toolsets: [a2a, clarify, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  aurora:
    role_class: design-author
    model: gpt-terra
    a2a_port: 9903
    production_write: false
    allowed_write_scopes: [design-dna, design-contract, research-evidence, candidate-hypotheses, content-map, asset-manifest, interaction-contract]
    allowed_root_toolsets: [web, browser, vision, file, skills, kanban]
    allowed_platform_toolsets: [a2a, browser, clarify, file, hermes-a2a, image_gen, kanban, session_search, skills, vision, web]
    forbidden_toolsets: [code_execution, computer_use, cronjob, delegation, terminal]

  forge:
    role_class: implementer
    model: gpt-terra
    a2a_port: 9904
    production_write: true
    allowed_write_scopes: [backend-source, systems-source, migrations, implementation-tests]
    allowed_root_toolsets: [file, terminal, code_execution, web, kanban, session_search]
    allowed_platform_toolsets: [a2a, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  frame:
    role_class: implementer
    model: gpt-terra
    a2a_port: 9905
    production_write: true
    allowed_write_scopes: [frontend-source, frontend-tests, build-artifacts, visual-spikes, spike-manifest, build-manifest, vertical-slice]
    allowed_root_toolsets: [coding, kanban]
    allowed_platform_toolsets: [a2a, browser, clarify, code_execution, file, hermes-a2a, image_gen, kanban, session_search, skills, terminal, vision, web]
    forbidden_toolsets: [computer_use, delegation]

  lens:
    role_class: verifier
    model: gpt-terra
    a2a_port: 9906
    production_write: false
    allowed_write_scopes: [qa-evidence, screenshots, defect-report, visual-tournament, slice-report, lens-review-report]
    allowed_root_toolsets: [browser, vision, web, file, terminal, code_execution, kanban]
    allowed_platform_toolsets: [a2a, browser, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, vision, web]
    forbidden_toolsets: [computer_use, delegation, image_gen]

  nexus:
    role_class: documentation
    model: ag-opus-pool
    a2a_port: 9907
    production_write: false
    allowed_write_scopes: [documentation, runbook, release-communication]
    allowed_root_toolsets: [file, web, todo, clarify, cronjob, session_search, kanban]
    allowed_platform_toolsets: [a2a, clarify, cronjob, file, hermes-a2a, kanban, session_search, skills, todo, web]
    forbidden_toolsets: [code_execution, computer_use, delegation, terminal]

  prism:
    role_class: verifier
    model: gpt-terra
    a2a_port: 9908
    production_write: false
    allowed_write_scopes: [test-source, test-results, benchmark-evidence, defect-report, functional-qa-report, prism-verification-report]
    allowed_root_toolsets: [file, terminal, code_execution, web, kanban]
    allowed_platform_toolsets: [a2a, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  quant:
    role_class: analyst
    model: gpt-terra
    a2a_port: 9909
    production_write: false
    allowed_write_scopes: [analysis-code, datasets, model-evidence, research-report]
    allowed_root_toolsets: [web, file, terminal, code_execution, kanban]
    allowed_platform_toolsets: [a2a, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  radar:
    role_class: researcher
    model: gpt-terra
    a2a_port: 9910
    production_write: false
    allowed_write_scopes: [research-evidence, source-ledger, research-report]
    allowed_root_toolsets: [web, browser, file, terminal, code_execution, kanban]
    allowed_platform_toolsets: [a2a, browser, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  sentinel:
    role_class: verifier
    model: gpt-terra
    a2a_port: 9911
    production_write: false
    allowed_write_scopes: [security-tests, scan-results, threat-model, defect-report]
    allowed_root_toolsets: [file, terminal, code_execution, web, browser, kanban]
    allowed_platform_toolsets: [a2a, browser, clarify, code_execution, file, hermes-a2a, kanban, session_search, skills, terminal, web]
    forbidden_toolsets: [computer_use, delegation]

  groupbot:
    role_class: isolated-chat
    model: ag-opus-pool
    a2a_port: null
    production_write: false
    allowed_write_scopes: []
    allowed_root_toolsets: [web, vision, tts]
    allowed_platform_toolsets: [web, vision, tts]
    forbidden_toolsets: [a2a, code_execution, computer_use, delegation, file, terminal]
