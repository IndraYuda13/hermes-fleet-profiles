---
name: fleet-orchestration-suite
description: Use when orchestrating multi-agent fleet pipelines & QA.
---

# Fleet Multi-Agent Orchestration, Governance & Defect Management

Comprehensive standard operating procedure for orchestrating specialized autonomous AI agent fleets (ORION, AURORA, FRAME, LENS, FORGE, ATLAS, PRISM, SENTINEL) across A2A pipelines, Kanban DAG dispatches, Impeccable design overhauls, and runtime defect rembukan.

## 1. Fleet Role Separation & Impeccable Ownership Matrix
| Fleet Role | Ownership Boundary | Impeccable Tools & Primary Deliverables | Execution Invariant |
|---|---|---|---|
| **ORION** | Chief of Staff, decomposition, governance, verdict synthesis | `routing`, `shape`, `craft-floor` review; Mission Contracts, Release Records | `orion_production_edits == 0` |
| **AURORA** | Product design authority, UI tokens, OKLCH color palettes | `init`, `concept-seed`, `palette`, `typeset`; `DESIGN.md`, `PRODUCT.md` | Design specs only; never implements code |
| **FRAME** | Frontend engineering, state machines, API integration | `typeset`, `layout`, `adapt`, `polish`; `styles.css`, `app.js`, HTML | Implements code; never self-certifies QA |
| **LENS** | Rendered visual/interaction QA & geometry verification | `audit`, `critique`, `contrast-rhythm`; `VISUAL_QA_REPORT.md`, viewport audits | Audits DOM; never edits production source |
| **FORGE** | Backend engineering, core business logic, worker daemons | APIs, SQLite queries, automation scripts | Backends & state machines |
| **ATLAS** | SRE, deployment, rollback anchors, process lifecycle | Systemd units, cgroup guards, rollback scripts | Infra stability & git anchors |
| **PRISM** | Functional QA, invariant assertions, test suites | Pytest suites, Playwright E2E tests | Strict test coverage; 0 false claims |

### Impeccable UI Invariants across Fleet
1. **Tabular Numeric Rule:** Always enforce `font-variant-numeric: tabular-nums` on prices, timers, nominals, and counters to prevent layout jitter during real-time polling.
2. **Glassmorphic GPU Isolation:** Pair `backdrop-filter: blur(20px)` with `contain: layout paint` and `transform: translateZ(0)` on navigation bars to prevent mobile compositing lag.
3. **Mobile Touch Safety Floor:** Interactive elements, buttons, category tiles, and form inputs must enforce a minimum 44×44px touch target geometry on mobile viewports (<=768px).
4. **Zero Horizontal Overflow:** Enforce `max-width: 100vw; overflow-x: hidden;` on body/root and verify across canonical viewports (320px, 360px, 390px, 768px, 1024px, 1440px).

## 2. A2A Communication & Timeout Management
- **Dual-Timeout Architecture:** Outbound client timeout in `config.yaml` (`a2a_agents.<peer>.timeout`) must strictly synchronize with inbound gateway deadline in `.env` (`A2A_REPLY_TIMEOUT`). Target: LENS/PRISM/SENTINEL/ORION: 1800s; FORGE/FRAME/AURORA: 1200s; ATLAS/RADAR/QUANT: 900s.
- **Progressive Decomposition:** Break large tasks into sequential multi-turn steps using `context_id` continuation (<300 words instruction per turn).
- **Concise Retry & Mechanical Fallback:** If a specialist times out, dispatch a concise 3-bullet retry. Split mechanical work (screenshot capture via FORGE) from judgment (visual audit via LENS).
- **Two-Way A2A Reverse Smoke Test Invariant:** Never assume bidirectional connectivity from outbound pings alone; verify the reverse direction (e.g. LENS -> ORION) using direct A2A JSON-RPC `SendMessage` calls.
- **Supervised Gateway Self-Restart Invariant:** Running agents are blocked by terminal guards from restarting their own gateway service directly. Request graceful restart via control socket or have the operator trigger it.
- **Evidence Manifest Git Revision Strategy:** Manifests committed inside git can NEVER contain their own commit SHA (avalanche effect). FRAME commits code (implementation SHA); all manifests reference that SHA; evidence artifacts are committed in a separate evidence commit.
- **Serial Incremental SDLC Invariant:** When mandated to work step-by-step ("1 per 1 dikerjakan, di-test, di-verifikasi"), tag initial state (`vX-baseline`), decompose into separate Kanban cycles (FORGE/FRAME -> PRISM/ATLAS -> commit/tag), and never batch multiple improvements into a single monolithic commit.
- **Remote MCP & Ad Monetization Engine:** See `references/ad-network-and-mcp-monetag.md` for OAuth 2.1 PKCE flow, Monetag MCP setup, and Service Worker push ad integration runbooks.

## 3. Defect Remediation Flow (Collaborative Rembukan)
- **Zero-Solo Invariant:** When runtime defects or upstream policy limits occur, ORION must never resolve them alone.
  1. *Discover & Isolate:* Trace logs and pin failure mechanisms.
  2. *Fleet Briefing:* Broadcast structured problem briefing via `a2a_orchestrate(capability="*")`.
  3. *Kanban DAG Dispatch:* Create discrete cards for logic (FORGE), UI (FRAME), infra (ATLAS), and QA (PRISM/LENS).
  4. *Independent Verification:* Implementer submits evidence; verifier confirms invariant pass before closure.
- **Kanban Task Creation, Listing & CLI Fallback:** For reading, commenting, and updating tasks, use native `kanban_show`, `kanban_comment`, and `kanban_list`.
  - *kanban_list Empty-String Parameter Trap:* In `kanban_list`, never pass empty strings `""` for optional filters (`assignee: ""`, `board: ""`, `tenant: ""`). Passing `assignee: ""` triggers validation failure `kanban_list: profile name cannot be empty`. Omit the argument entirely, pass a concrete profile name, or use `hermes kanban list --status <status> --json` or direct SQLite query on `/root/.hermes/kanban.db` to inspect the whole board across all profiles.
  - *Task Creation Fallback:* If native `kanban_create` encounters parameter signature mismatches (e.g. `missing required positional argument: 'title'`), use the deterministic Hermes CLI directly:
    `/usr/local/lib/hermes-agent/venv/bin/hermes kanban create --tenant "<TENANT>" --assignee "<ASSIGNEE>" --priority <PRIORITY> [--parent "<PARENT_ID>"] --workspace "dir:<PATH>" --body "<BODY>" --json "<TITLE>"`
    Always include `--json` for structured JSON output and verifiable task ID parsing.
  - *kanban_create 'completion_contract' Enum Trap:* When specifying `completion_contract` in the `kanban_create` tool, the contract format must strictly be `local-only` (default), the literal string `OWNER/REPO` (do not replace with the concrete repository slug), or an exact GitHub PR URL. Passing custom strings like `OWNER/<org>/<repo>` fails closed at parameter validation.
- **Whole-Board Nonterminal & Stuck Task Audit Protocol:**
  When checking for stuck Kanban processes or board health ("ada proses kanban yang nyangkut gak?"), do not stop at checking active worker processes. Disambiguate four distinct operational layers:
  1. *Layer 1 (Active Worker Scopes & Processes):* Inspect systemd transient worker scopes (`systemctl list-units --type=scope 'hermes-worker-kanban-*'`) and process list (`ps -eo pid,stat,args | grep '[h]ermes.*kanban'`). Verify whether any worker process is actually running or burning CPU.
  2. *Layer 2 (Dispatcher Daemon & Lock Provenance):* Check dispatcher lock holder (`fuser -v /root/.hermes/kanban/.dispatcher.lock`) and active gateway PID to ensure the dispatcher singleton is healthy and not orphaned.
  3. *Layer 3 (Database Nonterminal Triage):* Query nonterminal tasks (`sqlite3 -header -column /root/.hermes/kanban.db "SELECT id, status, assignee, priority, title, round((strftime('%s','now')-COALESCE(started_at,created_at))/3600.0,1) AS age_hours FROM tasks WHERE status NOT IN ('done','archived');"`). Categorize findings cleanly: (a) genuinely active tasks, (b) obsolete/superseded historical runs that should be archived (e.g. older backup attempts replaced by newer sealed SHAs), (c) legitimate external blockers requiring human decision (`needs_input`), and (d) unpromoted or un-dispatched ready cards.
  4. *Layer 4 (Orphaned Workspace Process Leaks):* Inspect active loopback listeners (`ss -ltnp`) and process tree (`ps -eo pid,ppid,lstart,etimes,args`) for long-lived background daemons, test servers, or development processes (e.g. `python3 -m http.server`, `vite`, `node`) anchored inside `/root/.hermes/kanban/workspaces/<task_id>/` that survived worker completion. Report or terminate orphaned workspace servers to prevent port collisions and memory leaks.
- **Kanban Live Claim Guard & Forced Completion (`--force`):** When reviewing or completing a task that is claimed or active under another worker run, `kanban_complete` without worker ownership credentials will fail-closed (`kanban_complete refused: task is running under a live worker claim`). When closing or overriding from an orchestrator/operator session after independent verification, pass `force: true` or run `/usr/local/lib/hermes-agent/venv/bin/hermes kanban complete --force <task_id>`.
- **Skill Manifest Pre-Validation & Bootstrap Crash Recovery Invariant:** When specifying `skills: [...]` during task creation, verify each skill is actively enabled in the target assignee profile (`hermes -p <assignee> skills list --source all --enabled-only`). Crucial: a skill may exist globally or on disk, but if it is listed in the target profile's `skills.disabled` (e.g. `hermes-agent` is disabled on `atlas`, `nexus`, `prism`, `quant`, `radar`), the dispatcher treats it as missing and crashes instantly at bootstrap with `Error: Unknown skill(s): <name>` (exit code 1).
  - *Default to Empty Skills (`skills: []`):* Because task card attributes (including skill pins) are strictly immutable once created, any unresolvable skill pin guarantees permanent bootstrap failure across all automatic dispatcher retries before any worker code or config is touched. Always default to empty `skills: []` (or omit skills) to inherit the target profile's configured enabled skills, unless an explicitly verified, enabled specialist skill is strictly mandatory.
  - *Non-Fatal Toolset Warnings vs Fatal Skill Errors:* Disambiguate bootstrap stderr: `Warning: Unknown toolsets: a2a, hermes-a2a` is a benign CLI warning caused by the dispatcher passing platform toolset metadata through to the CLI worker without filtering; it does NOT cause exit code 1 or worker termination. The fatal failure is strictly `Error: Unknown skill(s)`. Never mistake benign toolset warnings for the root crash cause, and never edit profile configs solely to suppress them.
  - *Systemic Dispatcher Hardening vs Config Mutation Invariant:* When bootstrap crashes or toolset warnings occur across the fleet, NEVER attempt quick-and-dirty fixes by mutating individual profile configurations (e.g. mass-enabling disabled skills or altering profile YAML files). In accordance with the Universal Engineering Standard, keep profile boundaries clean and intact. Route systemic fixes to the runtime dispatcher owner (`ATLAS`):
    1. *Dispatcher Pre-Spawn Skill Preflight:* Enforce validation at the dispatcher lane boundary (`_dispatch_lane_task`), after claim but *before* workspace materialization or worker subprocess spawn. Resolve `task.skills` against the assigned profile's effective enabled skill catalog (using `build_preloaded_skills_prompt` with profile-scoped `HERMES_HOME`). If any explicitly pinned skill is unavailable or disabled, immediately transition the claimed task to `blocked` (`kind: "needs_input"`) with a clear actionable reason without charging a failure counter or burning worker retries.
    2. *Worker CLI Toolset Derivation & Filtering:* When passing `--toolsets` to the CLI worker in `_worker_argv`, derive toolsets from the profile configuration but filter them through the runtime toolset validator (`validate_toolset`) to strip platform/metadata toolsets (`a2a`, `hermes-a2a`) at debug level, while preserving valid executable toolsets (`kanban`, `connections`, `terminal`, etc.). This eliminates harmless startup stderr warnings without mutating profile configurations.
    3. *Regression Test Enforcement:* Accompany all dispatcher fixes with isolated tests verifying disabled skill pins block cleanly without retries, valid pins reach worker argv, and toolsets filter properly.
  - *Immediate Supersession on Bootstrap Crash:* When a task crashes with `Error: Unknown skill(s)`, do not wait for the dispatcher to exhaust retries or misclassify the crash as a configuration, code, or QA regression. Because card attributes are immutable, retries cannot succeed. Immediately comment on the broken card explaining the bootstrap failure and spawn a replacement task with `skills: []` targeting the identical revision and acceptance criteria.
  - *Downstream Child Task Skill Sanitation:* When implementers (`forge`, `frame`) spawn downstream verifier cards (`prism`, `sentinel`), they frequently hallucinate procedural skills (e.g. `change-impact-verification` or category-prefixed paths). Verifier cards created for downstream verification must always use `skills: []` to rely on the verifier profile's built-in toolset and default prompt instructions.
- **Shared Worktree Serialization Invariant (`workspace_kind: "dir"`):** When multiple remediation tasks target the same shared repository directory (`workspace_kind: "dir"`), do not dispatch them concurrently. Concurrent workers sharing a single working tree risk dirty staging collisions, git index locks, and active data file pollution (e.g. test runs overwriting shared caches). Sequence parallel findings via parent dependency gating (`parents: ["<prior_task_id>"]`) so each subsequent remediation builds strictly on top of the preceding sealed commit.
- **Single-Writer Arbitration for Verifier-Spawned Tasks:** Independent verifiers (PRISM, SENTINEL) routinely generate child remediation cards upon discovering defects. When multiple verifier cards or orchestrator cards target the same scope or assignee in a shared worktree, ORION must actively arbitrate: designate one primary card as the sole writer, convert redundant or overlapping tasks into read-only evidence suppliers via comments, and instruct workers not to stage or commit source files concurrently.
- **Active Runtime State Hash Invariant & Test Isolation:** Verification gates and test reproduction scripts executed in shared workspaces must strictly isolate runtime data (e.g. using temporary `DATA_DIR`, ephemeral databases, or `*_PERSIST=false` flags). Verifiers must record SHA-256 hashes of persistent state files (such as `data/session_pool.json` and `data/credentials.json`) before and after test execution. Any unintended hash mutation is an immediate release blocker (`active_runtime_data_mutated`), even if all functional assertions pass.
  - *Test Harness Singleton/Persistence Isolation:* Repositories with file-backed persistence must enforce isolation at the root test harness (`conftest.py` or base fixture) using an isolated temporary directory and disabling persistence by default. Relying on external env flags or nested subprocess tests is insufficient because direct/bare `pytest` imports application singletons that bind to repository data paths.
  - *Attribution of Runtime Mutations:* Before assuming an active data mutation originated from live service traffic, inspect the file modification timestamp against service uptime and hash string literals against test fixtures. Test-driven fixture writes typically leave recognizable test strings and occur during test execution windows while the service is dead.
  - *Authorized Runtime State Disposition:* When resetting test-polluted state, never overwrite or delete with assumed `{}` or `[]`. Archive the exact existing bytes to a root-restricted directory (`0700` dir, `0600` archive/manifest) with matching SHA-256 hashes. Generate the replacement baseline using the application's actual reader/writer logic in a disposable path, verify it parses as empty, and atomically install it.
  - *Active-Service Probe vs In-Memory Cache Cleanliness:* When functional verification requires probing an already-active service daemon on loopback, live requests necessarily touch the daemon's in-memory session cache. Verifiers can verify persistent file immutability (unchanged SHA-256 of state files on disk), but cannot truthfully claim zero in-memory cache mutation. Verifiers must mark in-memory cache cleanliness as `UNVERIFIED` rather than failing the gate or asserting an unsupported clean state. If strict in-memory cache isolation is required, test against a disposable child daemon on a separate test port with persistence disabled.
  - *Isolated Loopback Candidate for Live Service Verification:* When functional verification requires live API checks (`CG_RUN_LIVE_TESTS=1`) while a production or owner-facing gateway daemon is actively running and serving traffic on its default port (e.g. `:8560`), never execute tests against that active listener. Probing an active service exercises its in-memory state, invalidating in-memory non-mutation claims and risking session pollution. Live suites must target a temporary isolated candidate process spawned on an ephemeral unused loopback port with a disposable `DATA_DIR` and `--lifespan off`, leaving the active service untouched, and proving port teardown upon completion.
- **Ephemeral Test Process Disambiguation & Mandatory Teardown Invariant:** When verifiers (PRISM, SENTINEL) execute live HTTP or client smoke tests against an isolated candidate, they may launch temporary background listener processes (e.g. `uvicorn` on loopback port 8560).
  - *Provenance Disambiguation:* An active listener found via `ss -ltnp` must not be blindly assumed to be the installed systemd service or an unauthorized deployment. Check process ownership and lineage (`ps -o pid,ppid,user,args -p <PID>` and `systemctl show <service> --property=MainPID,ActiveState`).
  - *Mandatory Teardown Before Task Completion:* Ephemeral test processes must be terminated before completing the verification task. The verifier must verify and report port closure (`ss -ltnp` empty for the port), prove the systemd unit remained `inactive`, and ensure active persistent state hashes are unchanged.
- **Staged Systemd Hardening & Offline Verification Invariant:** When proposing least-privilege systemd units (migrating from root to a dedicated unprivileged user, read-only `/opt` source, protected `/etc` config, `/var/lib` StateDirectory, `NoNewPrivileges`, namespace isolation, and syscall filters):
  - Propose unit files inside repository source (e.g. `systemd/<service>.service`) and document rollout/rollback in `docs/`.
  - Statically verify offline using `systemd-analyze verify <path>` and `systemd-analyze security --offline=yes <path>` to demonstrate exposure reduction without touching `/etc/systemd/system/`, reloading daemons, or starting the service.
  - *Decoupled Pre-Activation Verdicts:* Keep deployment and activation strictly decoupled from source review. For services requiring unprovisioned accounts/directories, verifiers must issue decoupled verdicts: `Source & Staged Hardening Candidate: ACCEPTABLE (for future window)`, but `Runtime Activation: BLOCKED` pending explicit host provisioning (dedicated user/group, `/opt` snapshot, protected `/etc` config, `/var/lib` state directory) and an activation-window smoke/retest at the exact same SHA.
  - *Authorized Activation Window & Fail-Closed Rollback Invariant:* After independent gates pass with decoupled verdicts (source acceptable, activation blocked):
    1. *Human Authorization Gate:* ORION must never trigger host mutation or service activation autonomously. Present an explicit authorization checkpoint defining the exact scope (non-root account provisioning, `/opt` source snapshot, protected `/etc` config, `/var/lib` StateDirectory, unit installation, daemon reload, loopback start, native smoke/retest, rollback plan).
    2. *Dedicated Host Mutation Dispatch (ATLAS):* Dispatch host changes as a single dedicated task to ATLAS with frozen interfaces: exact source SHA, staged unit path, baseline unit hash, and persistent state file hashes.
    3. *Zero-Secret File Placement:* Never copy secrets or place uninspected credentials into `/etc/`. Apply least-privilege modes (`0750` for dirs, `0600` for config/credentials, `0700` for StateDirectory).
    4. *Deterministic Native Smoke & Same-SHA Retest:* Immediately verify runtime identity is non-root (`User != root`), listener is strictly loopback, systemd is active, health/models/native client smoke (`hermes -p <profile> -z`) passes against loopback, and active data hashes remain unchanged.
    5. *Atomic Fail-Closed Rollback:* If any prerequisite, start, smoke, non-root check, or retest fails, immediately stop the service, restore baseline unit from recorded backup, daemon-reload, prove port closed, and report `ROLLED BACK / BLOCKED`. Never leave a half-provisioned or failing service running.
- **Native Client Smoke Classification (Sanitized Upstream Errors vs Functional Regression):** When running native client smoke tests (e.g. `hermes -p testing -z`) against an isolated gateway candidate:
  - *Sanitized Error Interception:* If the gateway correctly intercepts internal failures and returns sanitized public errors (`{"error": "Internal server error"}`), client tools will exit with an error. Verifiers must NOT falsely pass this as a successful client interaction.
  - *Root Cause Classification:* Distinguish whether the sanitized error is expected due to intentional absence of upstream credentials/network in the isolated test sandbox (in which case the native live smoke is `BLOCKED / NOT VERIFIABLE`, but sanitized error handling is `VERIFIED`), or an internal gateway routing/delimiter defect. Never substitute sanitized error responses for functional upstream pass evidence.
- **Test-Contract vs Implementation Defect Disambiguation on Intentional Reset Invariants:**
  - When verifiers (PRISM) report failures in live multi-turn continuity or regression suites (e.g. `CG_RUN_LIVE_TESTS=1 pytest`):
    1. *Inspect Contract Invariants First:* Check whether the failure stems from an intentional security/isolation contract (e.g. forcing session reset to root when a turn request carries an explicit session ID but omits prior assistant history, preventing cross-turn context or credential leakage).
    2. *Test-Contract Classification:* If runtime logic correctly enforces fail-closed isolation while the legacy test expected stateful carryover without history, classify this as a **test-contract defect** (`tests/`), not an application regression.
    3. *Release Gate Discipline:* Do NOT declare a release "stable" or unconditionally released while an optional or live test suite is failing. Report: "operational for normal use, but formal release gate remains BLOCKED / NOT RELEASED pending test-contract repair."
    4. *Promote Scoped Remediation:* Do not duplicate cards. Promote the already-scoped test repair task (e.g. to FORGE) with explicit requirements: update the test to pass proper full-history continuation, and assert a separate regression proving no-history requests reset cleanly.
    5. *Test-Only Commit Verification & Runtime Decoupling:* When an implementer commits a test-contract fix, verify that `git diff --name-only <parent_sha> <new_sha>` is strictly confined to `tests/`. Compare SHA-256 hashes of deployed server files (e.g. `/opt/<app>/server.py`) against repository source. When hashes match byte-for-byte, do not trigger service restarts, reloads, or redeployments. Resolving a functional test-contract blocker does NOT mitigate or lift concurrent security blockers (e.g. root-privileged daemons with broad toolsets); maintain fail-closed security gates until least-privilege remediation is independently verified.
- **Verifier-Remediation Dependency Inversion Trap (Deadlock Prevention):**
  - When an independent verifier (`PRISM`, `SENTINEL`) identifies a defect and generates or links a child remediation card (`FORGE`, `FRAME`):
    1. *No Indefinite Block Loop:* A verifier card's contract is to **render a definitive, evidence-backed verdict** (PASS or FAIL), not to wait until code is fixed. When defects are confirmed and child remediation is scoped, the verifier must NOT block indefinitely or wait in a loop. It must complete with an explicit verdict: `NOT RELEASED / FAIL pending <remediation_task_id>`. This completes the verifier run legitimately, allowing child remediation tasks that list the verifier as a parent to promote from `todo` to `ready`.
    2. *No Parent-Gating by Incomplete Verifier:* The remediation card must NEVER declare `parents: ["<failing_verifier_task_id>"]` unless the failing verifier is ready to complete with its FAIL verdict. If the verifier remains in `blocked` or `triage`, the child remains stuck in `todo (dependency_wait)`.
    3. *Orchestrator DAG Remediation:* If a dependency cycle occurs, ORION must immediately detach the failing verifier from the remediation card's parents (`kanban_link` / parent removal) or instruct the verifier to close with a non-release finding, promoting the remediation card to `ready` immediately.
    4. *Discrete Retest Task:* Once the remediation commit is sealed, commission a **fresh independent verifier task** targeting that exact new SHA. Do not attempt to resurrect or coerce the historical verifier card to pass.
- **Mid-Flight Scope & Policy Reconciliation for Independent Verifiers:**
  - When the operator or user alters capability or security requirements mid-turn (e.g. expanding an allowlisted profile from a minimal 6-tool subset to 12 full capabilities including terminal/browser/delegation):
    1. *Fail-Closed Verifier Invariant:* Independent verifiers enforce strict specification fidelity. If the original task card body contains restrictive negative assertions ("demonstrate no terminal/file/browser reaches profile"), the verifier will report `BLOCKED: contradictory acceptance criteria` rather than guessing whether a subsequent comment supersedes the task body.
    2. *Formal Policy Reconciliation Posting:* ORION must post a **Formal Policy Reconciliation** comment to the verifier's task thread: explicitly declaring the user's directive authoritative, identifying the superseded task-body clauses, and defining the exact new required and forbidden sets.
    3. *Unblock & Re-Evaluate:* After posting the formal reconciliation, unblock the verifier task (`kanban_unblock`) so the verifier can certify the implementation against the authoritative reconciled criteria.
    4. *Goal-Judge Stale-Body Trap on Reconciled Tasks:* In tasks dispatched with `goal_mode: true`, the auxiliary goal judge checks completion against the card's `title` and `body`, ignoring subsequent thread comments. If criteria expand mid-flight, a verifier's evidence-backed completion will be rejected by the goal judge, triggering `block_loop_detected` -> `triage`. Orchestrators must recognize this as an administrative artifact rather than a code defect: either update the card via specification if available, or administratively complete with `--force` / record final disposition to stop unblock loops.
    5. *Auto-Decomposer Stale-Body Inheritance Loop:* When a task with mid-flight reconciled acceptance criteria is triaged or auto-decomposed, the auto-decomposer inherits the root card's original immutable `body` into newly created child cards. Child workers (e.g. SENTINEL) then audit against that inherited stale text and fail/block immediately. When this occurs, ORION must immediately annotate each child task with the authoritative reconciled criteria and resolve the parent administratively, rather than letting workers repeatedly audit against stale inherited clauses or treating child blocks as security/functional regressions.
    6. *Administrative Acceptance Conflict vs Material Security Blocker Disambiguation:* When an independent verifier (SENTINEL) issues a BLOCKED verdict on a card that previously had administrative policy triage (such as a 6-tool vs 12-tool mismatch), ORION must inspect the exact finding mechanism rather than assuming it is another instance of the stale goal-judge loop. If the verifier reports an architectural or privilege violation (e.g. `ROOT_PRIVILEGED_TELEGRAM_TOOL_GATEWAY`), this is a genuine high-severity blocker, not an administrative text artifact. Never override, unblock, or force-complete a security gate when the underlying daemon violates least-privilege execution.
    7. *Auto-Triage Loop Suppression & Non-Runnable State Invariant:*
       - *Triage State Tool Guard (`kanban_block`):* The `kanban_block` tool only operates on cards in `running` or `ready` states; invoking it on a card currently in `triage` fails closed (`could not block: not in running/ready`). Never attempt to call `kanban_block` to halt a card in `triage`.
       - *Auto-Decomposer Loop Suppression via Board Directives:* When a verifier repeatedly blocks on a material defect requiring operator authorization (e.g. privilege reduction or containment), the board's `block_loop_detected` mechanism repeatedly kicks the task to `triage`, whereupon the auto-decomposer re-specifies and promotes it to `todo`, creating a perpetual audit cycle. To break this loop without modifying system state:
         1. Post an authoritative **Hold Directive** via `kanban_comment` on the task thread stating that the blocker is confirmed, holds until operator decision, and explicitly forbidding repeat read-only audits or configuration mutations.
         2. Verifier workers must inspect recent thread comments upon spawn; seeing an active hold directive, they must immediately record the held status and exit without re-running redundant diagnostic commands.
         3. *Notification Quieting & Actionable Steering on User Surface:* When repeated automatic task-status notifications arrive for an established hold, the orchestrator must not re-run redundant inspection commands (`systemctl`, `ps`, curl) or re-dump technical transcripts. Provide a concise acknowledgment that the hold remains active pending the owner's decision, accompanied by explicit, one-line actionable steering options (e.g. `migrasikan non-root` vs `matikan bot`) to enable the operator to break the loop cleanly.
- **Automatic Kanban Blocker Triage & Dependency Advancement Invariant:**
  - When receiving automatic status notifications (`[kanban] Task <id> blocked; needs attention`):
    - Never treat the alert as a prompt to re-decompose the mission or create duplicate task graphs.
    - Inspect the blocked card via `kanban_show`. Check whether it is an upstream verifier blocked on a defect for which it already created a child remediation task.
    - If the verifier is blocked because acceptance criteria failed, record the formal closing verdict on the parent card via `kanban_comment` (`NOT RELEASED pending <child_task_id>`) and unblock/complete the card so the dependency tree can advance the child task.
  - *Single Active Remediation Card on Review Rejection:* When a review returns a task with `changes_requested`, the task automatically returns to `ready`/`running` for the original implementer. Never spawn a duplicate remediation card. Maintain work strictly on the existing card. If a duplicate card is accidentally created, immediately cancel or force-complete it without mutations to maintain single-writer serialization on the shared repository.
  - *Review-Rejection & Worker Timeout Notification Discipline:* Automatic Kanban notifications for `review requested changes (BLOCK)` and `timed out; dispatcher will retry` are state confirmations, not prompts to re-decompose tasks or generate sibling cards. Review rejections return the existing task directly to the implementer (`changes_requested` -> `ready`/`running`); timeouts trigger automated retry runs by the dispatcher with workspace files preserved. Never treat rejection alerts as approvals, never create duplicate cards, and do not interrupt automated retries.
  - *Reviewer Worker Crash vs Artifact Invalidation (Replacement Review Routing):*
    - *Mechanism:* When a worker in the `review` lane crashes repeatedly (e.g. exit code 1 or unhandled worker exception) until retries exhaust (`gave_up` -> `status: blocked`), the failure is strictly an **infrastructure or reviewer execution failure**, NOT an implementer defect, NOT a failed acceptance gate, and NOT an artifact invalidation.
    - *Pitfall:* Do not treat a reviewer crash as a rejection or evidence defect. Never re-run the implementer's heavy baseline or implementation task from scratch, and do not unblock/retry a crashing reviewer profile in a tight loop.
    - *Procedure:*
      1. Verify that the implementer's deliverable artifact remains intact on disk with its recorded SHA-256 hash.
      2. Post a durable comment on the blocked task documenting that the reviewer crashed without rendering a verdict and that the underlying artifact remains intact.
      3. Dispatch a **narrowly-scoped replacement review task** to an alternative qualified specialist profile (e.g. PRISM for test-isolation and operational-contract review, SENTINEL for security review) with explicit instructions to evaluate the existing, unchanged artifact without re-running heavy host inspections or mutating runtime.
  - *Independently Recomputed Hash vs Declared Handoff Hash Invariant:*
    - *Mechanism:* Upstream task handoffs or card descriptions frequently copy artifact SHA-256 hashes that suffer from copy-paste drift, intermediate edits, or pre-modification values.
    - *Pitfall:* Verifiers blindly trusting a declared hash, or conversely failing a substantive review solely because a declared string in the prompt differed from the readable file.
    - *Procedure:* Independent verifiers MUST independently recompute SHA-256 hashes (`sha256sum <file>`) directly from disk bytes. All findings, verdicts, and downstream gates must be bound strictly to the **actually computed byte hash**. Explicitly record any hash discrepancy in the review artifact as an evidence-identity correction without assuming malice, and notify the orchestrator to correct the handoff manifest.
  - *Planning Baseline vs Implementation / Release Gate Segregation:*
    - *Mechanism:* Operational and environment baselines establish host facts, planning controls, and prerequisites; they are not staging environments or release approvals.
    - *Pitfall:* Treating an accepted or revised planning baseline as authorization to provision staging resources, enable providers, expose routes, or unblock implementation tasks prematurely.
    - *Procedure:* Mark baseline verdicts as strictly documentation/planning evidence (e.g. `ACCEPTABLE_AS_PLANNING_BASELINE` or `REVISE`). Retain all runtime gates (G-05/G-06/G-08, S-04/S-10/S-13) as `BLOCKED`. A planning approval does NOT unblock code or infrastructure mutation without a separate sealed-source SHA decision and explicit owner authorization.
  - *Sidecar Checksum File Pattern (`<artifact>.sha256`) for Planning Revisions:*
    - *Mechanism:* Baseline revisions and critical governance artifacts undergo multi-turn handoffs where file content drift or incomplete attachments can occur.
    - *Pitfall:* Verifiers reviewing stale workspace copies or unverified uploads, or relying on malformed checksum strings (such as 63-hex-char strings caused by off-by-one truncation).
    - *Procedure:* Pair every published revision artifact with an immutable sidecar file (`<artifact>.sha256`). The author generates it via `sha256sum <file> > <file>.sha256`. Orchestrators and independent re-reviewers must independently compute `sha256sum <file>` from disk, assert byte identity against the sidecar, and bind the review verdict strictly to that verified hash.
    - *Exact 64-Hex Character Validation & Malformed Sidecar Quarantine:* Checksum strings inside sidecars must be strictly validated for exact length (64 characters `[0-9a-f]{64}`). If an agent generates or uploads an incomplete sidecar (e.g. 63 characters due to terminal buffer or string slicing truncation), do NOT silently guess or accept it. Immediately flag the malformed sidecar as an *artifact-metadata defect* in a task comment. If a matching, valid 64-hex sidecar (e.g. `*_1.sha256`) is also present, bind downstream verifiers strictly to the valid sidecar and computed byte hash while documenting the quarantine of the defective sidecar.
    - *Reviewer Report Sidecar Mismatch vs Target Artifact Disambiguation:*
      - *Mechanism:* An independent verifier (PRISM, SENTINEL) completes its audit, correctly verifies the target artifact's SHA, and outputs a detailed review report (e.g. `PRISM_*_REVIEW.md`), but edits the report slightly after generating the sidecar or generates the `.sha256` sidecar from an earlier draft, causing `sha256sum -c` on the *review report itself* to fail.
      - *Pitfall:* Discarding the substantive review findings (e.g. 9 concrete required corrections) or stalling the entire DAG because the verifier's own report sidecar failed, or conversely blindly trusting the bad sidecar in downstream tasks.
      - *Procedure:*
        1. Disambiguate the target artifact from the review report. If the target artifact under review was independently verified and its hash matches, the target verification holds.
        2. If the review report text is intact, legible, and contains unambiguous required corrections, extract the findings as authoritative guidance for the next revision.
        3. Flag the sidecar discrepancy in a task comment as a **Verifier Report Metadata Defect** (recording actual report SHA vs declared sidecar SHA) to prevent downstream workers from attempting to verify against the invalid sidecar.
        4. Direct the downstream implementer (e.g. FORGE) to take the numbered findings as requirements while independently computing hashes from scratch, and require the subsequent review run to ensure both its target check and its own published report sidecar pass `sha256sum -c`.
    - *Rejected Artifact Supersession & Atomically Bound Basename Pattern:*
      - *Mechanism:* When a published artifact (contract, specification, governance document) is rejected with `changes_requested` / `REVISE`, the rejected file and its sidecar remain permanent historical attachments on the task card. To avoid filename collisions and preserve audit history, the implementer publishes the corrective version under an incremented, unambiguous basename (e.g. `<target>_R1.md`, `<target>_R2.md`) with a matching sidecar.
      - *Pitfall (Self-Referential Publication-Integrity Binding Mismatch):* The implementer updates the file body and saves it under the new filename, but leaves the document's internal publication-integrity section, check commands, or expected verification outputs bound to the old rejected basename (e.g. `<target>_R1.md` still declaring that the task publishes `<target>.md` and requiring `sha256sum -c <target>.md.sha256`). An independent verifier running the documented command validates the rejected historical file instead of the resubmission, triggering immediate review rejection.
      - *Procedure:*
        1. When superseding a rejected artifact under a new basename (`<basename>_R<N>.md`), update every internal self-referential filename, sidecar name, check command, and expected output string atomically to match the new basename before computing hashes.
        2. Explicitly document the superseded artifact's SHA-256 in a dedicated historical reference section marked as superseded evidence, not current target.
        3. Recompute the target file SHA-256 only after all internal text edits are complete to avoid self-hash mutation.
        4. Write exactly one strict GNU/LF sidecar (`64-hex + two spaces + exact new basename + LF`), verify `sha256sum -c` returns `OK`, attach only the new pair, and request review.
  - *Enforceable Egress & Notification-Isolation Planning Contract:*
    - *Mechanism:* Staging environments for financial or sensitive apps require airtight isolation to prevent accidental real-world side effects (charging real payment gateways, sending live customer emails, or leaking webhooks).
    - *Pitfall:* Relying on abstract policy prose ("deny-by-default outbound policy") or mock adapter flags alone without verifiable network boundaries.
    - *Procedure:* Before claiming isolated staging readiness, define an enforceable contract specifying: (1) named service identities with default-deny outbound posture (e.g. `stage-web`, `stage-worker`, `stage-postgres`, `stage-notify-capture`), (2) version-controlled kernel/network enforcement (nftables/internal Docker network), (3) explicit prohibited traffic classes (payment HTTP, SMTP, Telegram API, webhooks, outbound DNS, direct-IP), and (4) deterministic attempted-egress proof from each identity toward external synthetic decoys asserting zero external delivery and matching firewall counter increments.
  - *Host-Capability Observation vs Provisioning Feasibility Invariant:*
    - *Mechanism:* Host discovery tools can observe installed binaries (Docker, PostgreSQL client, Git) as empirical facts.
    - *Pitfall:* Confusing tool presence with proof that an isolated staging environment is feasible, performant, or safe to provision.
    - *Procedure:* Strictly classify observed local tools as `FACT / VERIFIED (tool presence only)`, but classify provisioning feasibility, storage headroom, image availability, permission boundaries, and isolation support as `INFERENCE / UNVERIFIED` until a dedicated, authorized provisioning exercise executes with empirical measurements.
  - *Brownfield Prototype Data Disposition Gate:*
    - *Mechanism:* When inheriting an unsealed or brownfield repository with pre-existing prototype schemas, test records, or mock balances, proceeding with backend implementation or migration contracts before resolving data disposition risks either unrecoverable data loss or wasted effort engineering backward-compatible migrations for throwaway data.
    - *Pitfall (Assumption & Jargon-Heavy Prompting):* Implementers either assume prototype records must be preserved (over-engineering backfills) or conversely assume they can be wiped without authorization. When prompting the owner for this decision, orchestrators frequently fail by using impenetrable governance jargon ("authoritative disposition for pre-existing prototype data in unsealed repository", "preserve with backfill", "disposable/non-production") that alienates the user.
    - *Procedure:* Explicitly require an authoritative owner disposition record (`PRESERVE_WITH_BACKFILL` vs `DISPOSABLE_IN_SANDBOX_ONLY`) before commissioning the single-writer implementation or migration lane (`FORGE`). Always explain the choice in plain, concrete terms: identify what actual data exists (products, user accounts, orders, wallet balances, stock) and ask directly whether existing demo/test data must be preserved with safe migration, or if it is purely disposable demo data that can be reset in a separate testing environment.
  - *Clarification Timeout Fail-Closed Guard on Irreversible Decisions:*
    - *Mechanism:* Interactive clarification prompts (`clarify`) to the human owner regarding data preservation, destructive actions, or launch gate overrides will expire if unanswered (`timed_out: true`).
    - *Pitfall:* Treating a timed-out prompt as implicit acceptance of the recommended choice or guessing the owner's intent to keep automated pipelines moving.
    - *Procedure:* Fail closed on clarification timeouts for material or irreversible boundaries. Record the unresolved decision on the task thread and review ledger, hold dependent implementation tasks in `todo` or `needs_input`, and present the concise decision point to the owner upon session wake.
- **Automated Task-Status Wake & Non-Decomposition Invariant:**
  - When an automated task-status wake arrives (`[kanban] Task <id> completed/blocked` with an explicit notice not to re-decompose):
    1. *Inspect Active Topology First:* Query `/root/.hermes/kanban.db` or run `kanban_show` to observe current tasks and in-flight workers across profiles before taking any action. Never re-decompose the mission, invent new parallel graphs, or recreate cards that already exist.
    2. *Artifact & Scope Ingress:* Read and evaluate the completed task's published artifact. Determine whether findings restrict or alter downstream contracts (e.g. security baseline findings declaring `DO NOT RELEASE` or provider research declaring an integration profile `UNRESOLVED`).
    3. *Selective Parent-Gated Promotion:* Only create or advance tasks that represent the next legitimate unfulfilled stage in the planned single-writer sequence. Bind dependencies using explicit `parents: ["<id>"]`.
- **In-Flight Asynchronous Constraint Injection via Thread Comments:**
  - When an asynchronous audit or research lane finishes (such as SENTINEL finding S-01..S-11 unauthenticated route blockers or RADAR clarifying provider documentation) while parallel architecture, design, or implementation workers (`FORGE`, `AURORA`, `FRAME`) are actively running:
    1. *No Blind Waiting:* Do not passively wait for active workers to complete deliverables based on obsolete or insecure assumptions.
    2. *Authoritative In-Flight Injection:* Immediately inject the newly verified findings into the active worker's task thread using `kanban_comment`.
    3. *Explicit Boundary Enforcement:* Explicitly direct backend workers (`FORGE`) to incorporate the security blockers into the contract freeze and proposed remediation sequencing; direct design workers (`AURORA`) to reject legacy insecure endpoints, enforce server-derived identity and step-up UI states, and avoid normalising unauthenticated payment settlement in interaction designs.
- **Pre-Commit Clean Diff & EOL Verification Invariant (`git diff --check` & `py_compile`):** Before committing or staging candidate fixes in a shared repository, always run `git diff --check`. Mixed CRLF/LF line endings or trailing whitespace introduced by targeted find-and-replace edits must be resolved with base-consistent EOL. When normalizing EOL or re-applying patches, verify that find-and-replace anchors did not miss targets due to line-ending mismatches (leaving variables or definitions uninitialized). Always run `python3 -m py_compile <files>` and confirm successful compilation before running test suites.
- **Post-Seal Parent Commit Diff-Check & Serial DAG Correction:** When an implementer seals a commit and claims completion, ORION must independently run `git diff --check <parent_sha> <sealed_sha>`. If commit-level defects (such as CRLF line endings, trailing whitespace, or unformatted code) are introduced:
  - Do not inject ad-hoc fixes into the shared worktree while a subsequent worker is already actively running.
  - Insert a discrete serial normalization task into the Kanban DAG directly following the active writer and gating subsequent downstream work.
  - Reparent affected downstream tasks via `kanban_link` to depend on the normalization task.
  - Instruct downstream verifiers (PRISM, SENTINEL) that the defective intermediate SHA is rejected as a clean release candidate and that release verification must bind exclusively to the consolidated post-normalization commit.
- **Worker Timeout Handling & Progress Preservation Invariant:** When a worker card times out (`elapsed > limit`), the dispatcher automatically schedules a retry run. A timeout is NOT a bootstrap crash (`Unknown skill`); the shared worktree retains the worker's in-progress source and test files. The orchestrator and retry worker must NOT discard partial progress or restart exploration from scratch. Immediately check `git diff --check`, compile modified files (`python3 -m py_compile`), and resume from the latest verified state toward sealing.
  - *Immediate In-Flight Steering on First Timeout (`dispatcher will retry`):* Do not wait for all retries to exhaust (`gave_up`) before intervening. As soon as the first timeout notification arrives, inspect `/root/.hermes/kanban/workspaces/<task_id>/` or the worktree in that same turn. If an artifact (specification, contract, or report) was already drafted or substantive diff exists, immediately post a steering comment (`kanban_comment`) with the file path, line count, and SHA-256, commanding the active retry run NOT to rewrite from scratch, but to adopt the existing draft, perform only final verification and sidecar generation, and complete. This prevents burning an extra 20–30 minute authoring run on duplicate work.
  - *Narrow-Scope Retry Disposition on Timeout / Exhausted Retries:* If retries are exhausted or a task is unblocked after long test suites, do NOT reset, stash, or recreate the task DAG. The existing worktree already contains the candidate patch and tests. Narrow the retry disposition via task comment: (1) inspect the existing diff against acceptance criteria, (2) run focused tests plus EOL/syntax checks, (3) verify untracked test files are included in evidence, and (4) request review immediately without restarting broad research or running full unrelated suites.
  - *Scratch Workspace Artifact Rescue on Authoring Timeout:* When an authoring worker (writing large specifications, contract revisions, or documentation artifacts) times out (`elapsed > limit`) before attaching the file or completing the task, the output file is often already completely written inside `/root/.hermes/kanban/workspaces/<task_id>/`. Before resetting or re-dispatching from scratch:
    1. Inspect the task's scratch workspace for completed files (`ls -la /root/.hermes/kanban/workspaces/<task_id>/`).
    2. Read and verify file completeness (check line counts, required sections, and compute SHA-256).
    3. Post a recovery comment on the task thread with the exact file path and verified hash.
    4. On retry or unblock, instruct the worker to adopt the existing artifact directly, perform sanity checks, attach it, and complete, rather than burning another 20 minutes re-authoring from scratch.
  - *Authoring Timeout Prevention via Structured Tables vs Verbose Narrative:*
    - *Mechanism:* Authoring workers tackling comprehensive state-machine contracts, authority matrices, or lifecycle indices (e.g. RC-01..RC-07) frequently burn their entire 1800s runtime budget when writing repetitive explanatory narrative prose, resulting in repeated timeouts (`elapsed > limit`).
    - *Pitfall:* Allowing authoring workers on retry to repeat broad background inspection or re-read massive earlier revisions from scratch, which guarantees another timeout.
    - *Procedure:*
      1. On retry or unblock, explicitly instruct the authoring worker to omit discursive narrative and write directly using compact, self-contained markdown tables: (a) closed command/capability enums, (b) permission/authority matrices, and (c) finite transition coverage indices (T01..Tnn).
      2. Forbid the retry worker from re-reading earlier revisions or broad context from scratch if inputs are already frozen in the task prompt.
      3. Tables provide stronger mechanical testability for verifiers (allowing automated regex/row parsing) while dramatically reducing authoring token count and runtime.
  - *Reviewer Timeout Artifact Rescue & Multi-Timeout Verdict Recovery:* When an independent reviewer (PRISM, SENTINEL) times out after conducting an exhaustive contract, security, or architecture audit, the worker frequently finishes writing the entire review report (`PRISM_*_REVIEW.md`, `SENTINEL_*_REPORT.md`) in its scratch workspace `/root/.hermes/kanban/workspaces/<task_id>/` right before the deadline. When retries exhaust (`gave_up` -> `status: blocked`), do NOT restart the 20-minute audit or spawn redundant review cards:
    1. Inspect the reviewer's scratch workspace for generated markdown reports.
    2. Read the full report and extract the independently verified verdict (`REVISE`, `BLOCKED`, `ACCEPTABLE_AS_*`), target SHA recomputation, and exact identified gaps.
    3. Post a comprehensive **Review Recovery Handoff** comment on the task thread documenting the report location, byte count, verified verdict, and specific gaps found.
    4. Unblock the task (`kanban_unblock`) and instruct the worker to adopt the existing report directly, compute and attach its valid SHA-256 sidecar, and submit formal completion with the verified verdict.
    5. *Truncated / Fragmented Attachment Detection on Multi-Timeout Recovery:* When a worker attempts to upload attachments near a deadline or across multiple timeouts, partial network writes or aborted tool calls can register truncated attachment fragments (e.g. 600–3,000 bytes or numbered duplicates `*(1).md`) on the task card while the authoritative complete file in the scratch workspace is much larger (e.g. 9–21 KB). Never blindly accept task attachments from timed-out runs. Compare attachment byte sizes and `sha256sum` against the workspace source. If attachments are truncated fragments, explicitly flag and quarantine them in thread comments, instruct the recovery worker to upload the exact verified complete workspace file, and ensure downstream tasks consume only the matching-hash full deliverable.
    6. *Orchestrator Durable Takeover on Repeated Uploader / Transport Failures:* If a reviewer or authoring worker successfully produced the complete report/contract on disk in its scratch workspace but repeatedly exhausts retries or times out solely during the uploader/attachment completion phase (e.g. base64 decoding errors, tool timeouts, or payload truncation):
       - Do NOT enter an infinite unblock loop or re-run the 20-minute analysis from scratch.
       - ORION directly verifies the workspace file (`sha256sum`, line count, completeness).
       - For review tasks, extract the explicit verdict (`REVISE` / `BLOCKED`), target artifact SHA, and numbered actionable defects.
       - Record an authoritative **Verdict Manifest** on disk/scratch and post a durable task comment binding the verdict and findings to the exact report hash.
       - Advance the dependency DAG immediately by creating the next remediation task (e.g. `FORGE WP01 R2`) with explicit corrective requirements and increased timeout (`max_runtime_seconds: 1800`), bypassing the broken uploader mechanism while maintaining 100% cryptographic and audit traceability.
  - *Untracked Test File Omission Trap in Review Evidence:* When a worker authors new test files in a repository workspace, standard `git diff --name-only` ignores untracked files (`??` in `git status`). Reviewers and implementers must run `git status --short` and `git diff --no-index --check /dev/null <untracked_test>` to ensure newly authored test files are tracked, compiled, and explicitly recorded in `changed_files` metadata before requesting review.
  - *Contract-Freeze Verification Loop & Pre-Implementation Determinism Gate:*
    - *Mechanism:* When engineering complex transaction, financial, or state-machine contracts prior to implementation (e.g. WP01), ambiguities in authority, lifecycles, or idempotency tuples cause downstream implementation divergences or security vulnerabilities.
    - *Pitfall:* Rushing into source code implementation, branch creation, isolated staging setup, or data resets while contract specifications still contain conditional authority words, unmapped lifecycle paths, or circular dependencies.
    - *Procedure:*
      1. *Artifact-Only Invariant:* Implementer (`FORGE`) and verifier (`PRISM`) must operate strictly on specification markdown files in task workspaces. Absolute zero edits to code, tests, configs, migrations, or live resources.
      2. *Mechanical Authority & Transition Binding (RC-01):* Every state transition across all mutable aggregates must map mechanically to a single named capability/command enum, actor identity, role grant/deny, expected version, and lock subset. Unbound transitions ('service-generated', 'by policy', 'as applicable') are strict determinism defects requiring `REVISE`.
      3. *Finite State & Fault Dispositions (RC-02):* Every mutable aggregate lifecycle must define finite transition edges covering all intermediate states (e.g. Cart `LOCKED` on quote expiry, Promo create/update, Deposit expiry/cancel/failure), and explicit dispositions for retries, stale writes, crashes, and late evidence. Mapping of payment receipts (e.g. `ALLOCATION_PENDING`) must be fully mapped to allocation outcomes without writer-invented state.
      4. *Deterministic Pre-Challenge Anonymous Scopes (RC-03):* Canonical idempotency tuples (K1) for pre-auth flows (e.g. anonymous account recovery) cannot depend on server-generated challenge nonces or unhashed PII. Define deterministic, privacy-preserving, one-way principal scopes before challenge issuance to eliminate circular K1 dependency.
      5. *Fail-Closed Review Response:* An independent verdict of `REVISE` strictly bars code initiation. Do not proceed to source branch, worktree creation, isolated staging setup, or data reset until `ACCEPTABLE_AS_CONTRACT_BASELINE` is independently certified at an exact SHA.
      6. *Mechanical Parser & Cardinality Self-Consistency Gate:* Machine-checkable state machines, authority matrices, and capability catalogs require exact cardinality and deterministic parser rules.
        - *Cardinality Drift:* Every reference to total row count, inherited numeric rows, retired rows, and appended suffixed rows must sum identically across every section, enum list, matrix table, and parser requirement. Conflicting counts across paragraphs (e.g. 108 table rows vs "ten named rows" or "110-row set") are strict determinism defects.
        - *Grammar/Parser Contradiction:* Prescribing a parser rule (such as "split column on `=`") that does not match the actual syntax of table cells (such as single backticked values `` `svc.identity` `` with zero `=`) prevents mechanical verification. Always test parser rules directly against the table markdown before publishing.
- **Strict Protocol Hardening vs Custom Framing Delimiter Preservation:** When hardening parsers to enforce strict per-turn capability binding against foreign/replayed tokens (e.g. `<<<TOOL_CALL_<nonce>>>`), restrict suppression specifically to reserved protocol tokens. Do not blanket-suppress arbitrary user or test custom framing delimiters (e.g. `<<<START>>>` / `<<<END>>>`); verify that neighboring fragmented and non-protocol delimiter contracts remain functional.
- **Strict Anti-Polling Invariant & Native Event-Driven Session Wake:**
  - *Zero Busy-Wait & Autonomous Self-Discipline:* ORION must NEVER run sleep-and-check polling loops in terminal (`sleep 30` -> `kanban_show` -> `sleep 30`, or bash `while` loops) while awaiting background worker completion. Polling wastes tool calls, burns foreground timeouts (600s), blocks user interaction, and violates reactive architecture. Crucially, ORION must NEVER ask the user to remind it not to poll ("ingetin gua ya bre"); self-discipline is maintained autonomously via skills.
  - *Hermes Core Native Wake Architecture (No Third-Party Plugins):* Do not install or rely on third-party `self-wake` plugins. Hermes Agent core already features native session wake (`gateway/wake.py` and `delivery_mode="notify+wake"`).
  - *CLI Fallback Subscription Registration Invariant:* When creating a task via the Hermes CLI fallback (`hermes kanban create`), the CLI subprocess does not inherit the ambient gateway session ContextVar, so `kanban_notify_subs` is not automatically populated. ORION MUST immediately register the subscription explicitly:
    `/usr/local/lib/hermes-agent/venv/bin/hermes kanban notify-subscribe --platform <PLATFORM> --chat-id "<CHAT_ID>" --chat-type <CHAT_TYPE> --notifier-profile <PROFILE> --delivery-mode notify+wake <TASK_ID>`
    (e.g., `--platform telegram --chat-id "696907598" --chat-type dm --notifier-profile orion --delivery-mode notify+wake <TASK_ID>`).
  - *Mandatory Turn Release:* After dispatching Kanban tasks and ensuring active `notify+wake` subscription, ORION MUST report the dispatch status clearly and conclude the turn immediately. Do not hang the session waiting. The live gateway will natively wake ORION's session the exact second the worker reaches a terminal event (`completed`, `review_requested`, or `blocked`), injecting the event payload into the conversation turn.
- **Consolidated Release Candidate Parent-Gating Invariant:**
  - Verifier tasks (`PRISM`, `SENTINEL`) that gate release decisions must NEVER be scheduled or dispatched against intermediate remediation commits while upstream remediation tasks are still active or queued.
  - Independent verifier cards must strictly declare `parents: ["<terminal_remediation_task_id>"]` to remain in `todo` until the final consolidated candidate SHA is sealed.
  - If DAG adjustments (such as EOL normalization or secondary defect fixes) insert intermediate cards, ORION must immediately call `kanban_link(parent_id="<new_predecessor>", child_id="<verifier_task_id>")` so verifiers do not prematurely execute against stale or defective predecessor commits.
  - *Parent Task ID Validation Gate:* When constructing parent-gated child tasks (`parents: ["<id>"]`), always capture and verify the exact resolved predecessor task ID from the database or command output. Never pass assumed, hardcoded, or placeholder IDs; `kanban_create` fails-closed (`unknown parent task(s)`), stalling DAG promotion.
- **Mid-Turn Progress Synthesis Architecture (Plain Language Invariant):**
  - When communicating progress, status, or decisions to the user ("progressnya udah sampai mana?", "gimana bre?"), speak in plain, relaxed, conversational Indonesian (santai & to-the-point). Strictly avoid pretentious governance jargon, abstract engineering phrases, or ungrounded status dumps (e.g. "fase discovery", "contract-freeze proposal", "operational readiness baseline", "security baseline", "disposable prototype vs preserve with backfill") which trigger immediate operator frustration ("gak usah pake bahasa yang susah dipahami, gw gak suka!!").
  - *Zero Bare-Jargon Rule:* Never use English SDLC or governance terms (`preserve`, `disposable`, `baseline`, `contract-freeze`, `remediation`, `gates`) in conversational updates without immediately translating or grounding them in plain everyday Indonesian terms:
    - `data preserve vs disposable` -> "data percobaan/demo yang ada sekarang (akun tes, produk dummy, saldo palsu) mau disimpan atau boleh direset/dibuang?"
    - `contract freeze / WP01` -> "dokumen aturan main transaksi & anti-duplikat pesanan sebelum koding"
    - `independent review REVISE` -> "pengecekan independen nemu celah di dokumen aturan, lagi diminta revisi"
    - `read-only baseline / WP00` -> "pengecekan awal kondisi sistem tanpa sentuh atau ubah kode apa pun"
    - `remediation sprint` -> "tahap perbaikan koding/backend"
  - *Forbidden vs Required Phrasing Examples:*
    - ❌ *FORBIDDEN:* "Sekarang masih di fase discovery, contract freeze, security baseline, dan operational planning."
    - ✅ *REQUIRED:* "Lagi beresin desain dan dokumen aturan main transaksi. Belum masuk koding aplikasi."
    - ❌ *FORBIDDEN:* "Memerlukan authoritative disposition apakah data prototype disposable atau preserve with backfill."
    - ✅ *REQUIRED:* "Data demo yang ada sekarang (akun tes, produk dummy, saldo palsu) mau dibuang/reset atau mau disimpan?"
  - Never dump internal SDLC phase names or bureaucratic governance labels (e.g. "fase discovery, contract-freeze, security baseline, dan operational planning"); describe what is actually happening in plain conversational Indonesian (e.g. "riset & desain sudah beres, dokumen kontrak sudah jadi dan lagi dicek celah/aturannya, koding perbaikan belum mulai").
  - Provide a structured, accessible brief:
    1. *Status Utama (1 Kalimat Santai):* Posisi pengerjaan saat ini secara gamblang dan langsung ke inti.
    2. *Completed & Proven:* What is already done and verified in plain terms (e.g. riset & desain selesai, dokumen kontrak beres).
    3. *Actively In-Flight & Blocker Explanation:* What the active worker is doing right now and why delays/timeouts happen in simple language (e.g. "PRISM lagi cek 4 celah di kontrak; sempat timeout pas kirim file tapi dokumennya aman").
    4. *Remaining Path to Release:* What hasn't started yet (e.g. koding perbaikan, testing) and what remains blocked (e.g. belum boleh deploy/live).
    5. *Containment & Safety Reassurance:* Explicitly reassure that live/production databases, servers, code, or active services have not been touched or modified.
    6. *Plain Grounding for Choices:* When presenting choices or asking for operator direction, immediately translate technical dilemmas into real-world everyday objects without jargon (e.g. instead of "authoritative disposition for prototype data", ask: "data percobaan yang sudah ada sekarang kayak akun demo, produk tes, dan saldo dummy mau dibuang/reset atau mau dipertahankan?").

## 4. Production UI Overhaul Governance & Rollback Anchors
- **Dynamic Git Anchors:** Before modifying production source, record exact branch (`git symbolic-ref --short HEAD`) and SHA (`git rev-parse HEAD`), create a dedicated feature branch, and author an automated 1-click rollback script (`rollback_to_vX_stable.sh`).
- **Feature-Level Rollback Verification:** Rollback verification must confirm feature signatures (canvas, WebGL, scripts) in a headless browser, not merely HTTP 200 or title tags.
- **Release Gating:** Two independent verification passes (PRISM functional + LENS visual) required before ORION presents deliverables for human review.

## 5. UI Design V4 Lifecycle & Verification Hardening
For UI Design Depth 2 and Depth 3 missions, ORION enforces the full 14-stage V4 pipeline, Creator != Certifier boundary, bounded exploration budget, and dual independent release veto.
See complete procedural guide and invariant rules:
`skill_view("fleet-orchestration-suite", "references/ui-v4-orchestration-and-verification-governance.md")`

## 6. Asynchronous Dual-Verifier Arrival & Release State Machine
- **Asynchronous Verifier Arrival Handling:** When independent verifiers run in parallel (e.g. LENS visual QA and PRISM functional/a11y QA), one verifier will inevitably finish and report first via A2A or Kanban.
  1. *Early Arrival Acknowledgment:* Acknowledge receipt, inspect on-disk artifacts (`LENS_REVIEW_REPORT.json` or PRISM test logs), and approve promotion to `BEST_BUILD_SHA` for that verification dimension.
  2. *Strict In-Flight Guard:* Record `INTERNAL_VISUAL_GATE = PASS` (or functional pass), but keep `MISSION_RELEASE_STATE = IN_VERIFICATION` while sibling verifier tasks remain active (`status: running`). Never declare mission release or notify the human owner until ALL required verifier tasks reach terminal states.
  3. *Owner Acceptance Reservation:* `INTERNAL_RELEASE_GATE = PASS` marks internal verification closure only. `OWNER_VISUAL_ACCEPTANCE` and `OWNER_ACCEPTANCE` MUST remain `PENDING`. Internal agents (ORION, LENS, PRISM, FRAME) are strictly forbidden from setting owner acceptance to `PASS`; only the human owner can transition it.
  4. *Unified Meta-Release Synthesis:* Once all verifiers complete with PASS, ORION synthesizes the unified release decision record, bundles all visual and functional evidence, and presents the deliverable with `MISSION_RELEASE_STATE = AWAITING_OWNER`.

## 7. Declarative Fleet Upgrades, Gateway Lifecycle & Terminal Safety
- **HERMES_HOME Subprocess Scope Trap:** When running deployment or bootstrap scripts from inside an active profile session (such as `orion`), `$HERMES_HOME` is exported in the environment as `/root/.hermes/profiles/<profile_name>`. Scripts defaulting to `${HERMES_HOME:-$HOME/.hermes}` will mistakenly target the subprofile directory, polluting it with nested `profiles/` directories. Always explicitly prepend `HERMES_HOME="${HOME}/.hermes"` when invoking root fleet scripts or bootstrap tools from an active agent session.
- **Terminal Guard Lifecycle Choke Point & Sibling Gateway Restarts:** Running `systemctl --user restart hermes-gateway-<profile>.service` directly is blocked by Hermes's terminal lifecycle guard (`contains_gateway_lifecycle_command`), which matches `hermes-gateway` and `systemctl.*restart`. To restart sibling gateways safely without triggering the terminal guard, use the profile-flag CLI form: `hermes -p <sibling_profile> gateway restart`. The lifecycle guard explicitly permits restarting sibling profiles (`_named_profile_is_current` check allows sibling operations while blocking restarts of the executing profile). Never forcefully restart one's own current gateway profile mid-turn, as SIGTERM terminates the active child process before delivering the response.
- **Hermes Core Runtime Patch Activation & Host-Scoped Gateway Reload:**
  - When an approved patch modifies core Hermes Agent runtime code (e.g. in `/usr/local/lib/hermes-agent`):
    1. *Local Commit Scope:* Stage only the exact reviewed paths; run `git diff --cached --check`; commit locally with descriptive conventional message; do NOT push upstream unless explicitly authorized by the operator.
    2. *Host-Scoped Gateway Reload:* Never restart all fleet profile gateways indiscriminately. Dynamically identify which gateway currently owns the dispatcher singleton lock `/root/.hermes/kanban/.dispatcher.lock` (e.g. via `fuser` or inspect running gateways with effective `dispatch_in_gateway: true`). Do not assume `orion` or any single profile is permanently the host. Indiscriminately restarting uninvolved specialist gateways interrupts active worker processes or resets ephemeral messaging/platform sessions.
    3. *Transient Worker Scope Topology:* Active Kanban workers execute inside systemd transient scopes (`hermes-worker-kanban-*.scope`) outside the gateway's systemd cgroup. Therefore, restarting the confirmed dispatcher gateway safely reloads the dispatcher loop without killing in-flight worker scopes.
    4. *Parent-Gated Verification Safety:* Always gate independent post-activation verification tasks (`parents: ["<activation_task_id>"]`) using the exact verified parent task ID, never unverified or placeholder IDs. `kanban_create` fails-closed on unknown parents.
    5. *Upstream Publication Verification & Divergence Guard:* When authorized to push an activated local commit upstream, never run blanket staging (`git add .`) that could pick up unrelated untracked `.bak`, session, or traffic dump files. Run `git fetch origin <branch>` and inspect divergence (`git rev-list --left-right --count origin/<branch>...HEAD`). If divergence shows remote commits ahead (`left > 0`), the local clone is diverged or lagging upstream: immediately block with `needs_input` and report exact divergence counts. Never attempt force-push, merge, or blind rebase on a live system library installation (`/usr/local/lib/hermes-agent`). Disambiguate for the operator: upstream core code (`NousResearch/hermes-agent`) cannot accept direct pushes to `origin/main` when diverged and belongs on a separate fork/PR branch; it is completely separate from the user's fleet profile configuration backup (`IndraYuda13/hermes-fleet-profiles`). If divergence is strictly `0 1` (clean fast-forward), execute `git push origin HEAD:<branch>`, and immediately re-fetch to prove remote ref byte-matches the target SHA.
- **Declarative Git Pulls vs Runtime Secret Preservation:** Git pulls from declarative configuration repositories (`hermes-fleet-profiles`) contain sanitized placeholders (`${MONETAG_API_TOKEN}`). Never use `--replace-config` blindly on live environments. Deploy declarative assets (`SOUL.md`, `profile.yaml`, custom/global skills) first, then surgically update targeted non-secret delta keys (e.g. `skills.disabled`) while preserving secrets byte-for-byte.
- **Fleet Model Re-Routing & Verification Invariant:** When changing default inference models across fleet profiles:
  1. *Verify Active Endpoints:* Query the local router/gateway first (`curl -s http://localhost:20128/v1/models | jq -r '.data[].id'`) to verify exact upstream model IDs (`gpt-sol`, `gpt-terra`, `ag-opus-pool`) before applying.
  2. *Surgical Per-Profile Update:* Use `hermes -p <profile> config set model.default <model_id>` to ensure atomic YAML updates without formatting corruption.
  3. *Audit State Before Restart:* Run `hermes profile list` to verify that each profile's model column matches the intended role-model topology.
  4. *Gateway Restart Guard:* Never attempt `hermes gateway restart --all` or restart the executing profile from within an active session—it is blocked fail-closed by Hermes terminal guards. Either delegate the restart to the human operator outside the gateway or restart sibling profiles individually via `hermes -p <sibling> gateway restart`.
- **Multi-Surface Profile Capability Expansion & Platform Boundary Invariant:**
  - When the operator expands a profile's toolset (e.g. granting `terminal`, `file`, `browser`, `code_execution`, `web`, `delegation` across all execution contexts):
    1. *Apply Across All Callable Surfaces:* Use official Hermes CLI commands (`hermes --profile <name> config set ...`) to configure the exact authorized list across global `toolsets`, `platform_toolsets.cli`, `platform_toolsets.telegram`, and `platform_toolsets.a2a`. Never edit YAML files directly.
    2. *Strict Boundary Isolation:* Expanding functional toolsets must NEVER weaken platform authorization boundaries. Telegram user allowlists must remain strictly pinned to numeric owner ID (`TELEGRAM_ALLOWED_USERS="<id>"`), maintain `allowed_chats: ["__dm_only__"]`, `group_policy: disabled`, and `guest_mode: false`.
- **Root-Privileged Tool Gateway & Least-Privilege Daemon Invariant:**
  - When a profile is granted execution toolsets (`terminal`, `file`, `browser`, `code_execution`, `web`, `delegation`), its background gateway service (e.g. `hermes-gateway-<profile>.service`) must NEVER run as root (UID/EUID 0).
  - *Post-Auth Blast Radius:* A strict Telegram DM allowlist (`dm_policy: allowlist`, single owner ID) and restrictive 0600 file modes only guard the pre-authentication edge; they do NOT mitigate post-auth blast radius. A compromised owner session, prompt injection, or upstream model manipulation can execute arbitrary shell commands with root privileges across the entire host.
  - *Mandatory Unprivileged Sandboxing:* Any gateway exposing shell or code execution tools must run under a dedicated unprivileged service account with systemd sandboxing (`NoNewPrivileges=true`, `ProtectSystem=strict`, isolated writable working directory) and non-writable root-owned configuration. Independent verifiers (SENTINEL) must assert active daemon `EUID != 0` before granting release approval.
- **Mixed Sys.Modules & In-Memory Daemon Drift Post-Update:** Running `hermes update` modifies CLI libraries on disk but does not restart active background gateway daemons. In-flight daemon workers continue executing pre-update modules in memory (`mixed sys.modules`), producing subtle runtime mismatches between CLI behavior and daemon tool invocation. Before certifying declarative synchronization or runtime upgrades, verify gateway uptime and schedule sibling gateway restarts (`hermes -p <profile> gateway restart`) or operator-level gateway restart.

## 8. Bi-Directional Fleet Profile Synchronization & Remote Backup (hermes-fleet-profiles)
When auditing or persisting live fleet policy, SOUL.md updates, custom skills, or model changes back to the version-controlled fleet repository (`/root/projects/hermes-fleet-profiles` -> `IndraYuda13/hermes-fleet-profiles.git`):
1. **Two-Layer Fleet Backup Verification & Repository Disambiguation (Git Remote vs Live Runtime Drift):**
   - *Zero False-Backup Invariant:* Never claim the fleet is fully backed up based solely on `git status` (clean worktree) and `HEAD == origin/main`. Git only tracks files already synchronized into the repository working tree.
   - *Three-Layer Storage Disambiguation Invariant:* When the operator asks whether the fleet is backed up or matches current state ("sudah dibackup belum? apakah sudah kayak kondisi kita saat ini?"), explicitly disambiguate the three distinct storage layers before answering:
     1. *Core Framework Runtime (`/usr/local/lib/hermes-agent`, upstream `NousResearch`):* The underlying application engine; tracks runtime codebase and dispatcher fixes, not fleet profiles or user configuration.
     2. *Fleet Profile Backup (`/root/projects/hermes-fleet-profiles` -> `IndraYuda13/hermes-fleet-profiles.git`):* The user's version-controlled declarative fleet configuration (SOUL prompts, configs, curated skills, governance policies).
     3. *Local Recovery Snapshots (`/root/projects/hermes-fleet-backups/YYYYMMDDTHHMMSSZ`):* Point-in-time filesystem rollback archives on host disk.
   - *Point-in-Time Snapshot vs Live Mirror Semantics:* A Git backup is a point-in-time sealed capture, not an automatic real-time mirror. Active profile daemons continuously generate telemetry (`.usage.json`), skill locks, and session state. Never answer with a simple binary "yes" without reporting both dimensions:
     - *Snapshot Remote Parity:* Whether the last sealed snapshot is pushed to remote (`ahead/behind: 0/0`, commit SHA, commit date).
     - *Live Runtime Drift:* Whether live `/root/.hermes` has drifted from the repository baseline (run `sync.sh` dry-run and state the count of proposed sync rows).
   - *Layer 1 (Git Remote Parity):* Run `git fetch && git status -sb` and verify ahead/behind count between `HEAD` and `origin/main`.
   - *Layer 2 (Live Runtime Drift Probe):* Execute `HERMES_HOME=/root/.hermes bash scripts/sync.sh` (dry-run) to detect un-synced runtime mutations across `/root/.hermes/profiles/*`.
   - *Layer 3 (Destructive Deletion Accounting & Curator Disambiguation):* Parse proposed change lines (specifically deletions vs additions/modifications). When mass skill deletions appear in a dry-run (`*deleting profiles/<name>/skills/...`):
     - *Repo Deletion vs Fleet Loss Distinction:* A dry-run deletion means the repo contains files *no longer present on the live profile disk*. It is not `sync.sh` deleting live files.
     - *Curator Umbrella Consolidation Audit:* Check the profile curator ledger (`hermes -p <profile> curator ledger --limit 30`) and status (`hermes -p <profile> curator status`). Determine whether removed skills were intentionally absorbed into class-level umbrellas (e.g. `domain-ux-architecture` on AURORA, `software-development-workflows` on FORGE/PRISM, `fleet-orchestration-suite` on ORION, `service-api-verification` on PRISM).
     - *Global vs Profile Inheritance Verification:* When profile copies of global skills are pruned, do not assume profile inheritance is automatic. Always verify active availability with `hermes -p <profile> skills list --source all --enabled-only`.
   - *Layer 4 (Snapshot Recovery Provenance):* Verify recent timestamped snapshots exist in `/root/projects/hermes-fleet-backups/` before performing any apply or remote push.
   - *Layer 5 (Host Profile Inventory Coverage Gate):* Compare the hardcoded profile manifest (`PROFILES` array in `scripts/sync.sh`) against the active host inventory (`/root/.hermes/profiles/*` and `hermes profile list`). Ephemeral or auxiliary profiles (e.g. `testing`, experimental sandboxes) present on disk but omitted from `sync.sh` are unmanaged scope gaps that must be explicitly reported rather than silently ignored.
   - *Sanitized Declarative Equality vs Raw Disk Byte Identity Invariant:* Never evaluate raw config equality using direct file hashes (`sha256sum`). The declarative repository purposefully sanitizes secrets, tokens, basic authentication, and platform user/chat IDs (`sanitize_config.py`). Parity exists when the *sanitized* live configuration matches the repository baseline byte-for-byte; raw host configs are expected to differ. Explicitly declare that runtime secrets (`.env`), databases (`*.db`), session transcripts (`*.jsonl`), root config, and gateway memory states (`mixed sys.modules`) sit outside repository scope by design.
   - *Declarative 1:1 Parity Audit & Delegation Protocol:* When the operator asks whether the repository is 1:1 with the current live fleet ("udah 1:1 kayak kondisi hermes sekarang belum?"):
     1. *Disambiguate Intent:* Clarify that the repository is a version-controlled declarative blueprint (prompts, sanitized configs, curated skills, governance policies), not an unencrypted runtime clone. State the excluded categories (`.env`, `auth.json`, `state.db`, `kanban.db`, `cron`, sessions, logs).
     2. *Three-Tier Parity Audit:* Check (a) SOUL/prompts (direct comparison), (b) sanitized configs (`sanitize_config.py` + `apply_role_policy.py` vs committed YAML), and (c) skill drift via `sync.sh` dry-run.
     3. *Delegated Sync Execution:* When authorized to sync, dispatch a single-writer Kanban task to `ATLAS` with `workspace_kind: "dir"` at `/root/projects/hermes-fleet-profiles`. ATLAS executes the staged snapshot (`sync.sh --apply`), runs test suites (`validate_fleet.py` + unittests), performs secret scanning on git additions, and pushes to remote, gated by independent verifier audit.
   - *Static Declarative Drift vs Dynamic Telemetry Disambiguation:* When auditing `sync.sh` dry-run outputs, parse and separate noise: dynamic telemetry files (`.usage.json`, `.bundled_manifest`) and transient lockfiles (`.locks/`) churn continuously during normal multi-agent operation. Only flag and track genuine declarative drift in static skill files (`SKILL.md`, supporting docs/scripts) or configuration policy changes.
2. **Curator Runtime Artifact & Telemetry Hygiene:**
   - *Rsync Staging Exclusions:* Sync tooling must strictly exclude ephemeral runtime directories (`--exclude='.curator_backups/' --exclude='.locks/'`) during rsync to avoid staging megabytes of local backup tarballs and lockfiles into the git working tree.
   - *Telemetry vs Declarative Versioning:* Dynamic curator state files (`.curator_state`, `.curator_suppressed`, `.usage.json`, `.bundled_manifest`) churn continuously due to background telemetry and usage counters. Keep git focused on declarative policies (`config.yaml`, `SOUL.md`, `profile.yaml`, `SKILL.md`) to prevent noisy, non-functional commit churn.
3. **Operational vs Declarative Config Delta Audit:**
   - Deep-diff all modified `config.yaml` files before staging.
   - Distinguish benign schema bumps (e.g. `_config_version: 44 -> 45`, registered builtin toolset lists) from high-impact operational changes (e.g. enabling messaging platforms `platforms.whatsapp.enabled: true`, open group access policies, modified bridge ports, or disabled plugins). Treat operational platform mutations as explicit deployment decisions requiring separate review, not incidental backup diffs.
4. **Model & Role Topology Alignment:**
   - The validation engine (`scripts/validate_fleet.py`) enforces that `profiles/<name>/config.yaml` model defaults match `governance/roles.yaml`.
   - If profile models change (e.g. migrating Sol/Terra/Opus allocations), update `governance/roles.yaml` to match before running validation.
5. **Declarative Synchronization Execution:**
   - Always run with root Hermes home: `HERMES_HOME=/root/.hermes bash scripts/sync.sh` (dry run inspection) followed by `HERMES_HOME=/root/.hermes bash scripts/sync.sh --apply`.
   - `sync.sh --apply` automatically stages declarative changes into `profiles/` and `global/skills/`, creating an immutable timestamped snapshot in `/root/projects/hermes-fleet-backups/YYYYMMDDTHHMMSSZ`.
   - *Capability Routing & Worktree Cleanliness Invariant (`sync.sh --apply`):*
     - *Tool Capability Guard:* `sync.sh --apply` executes bash and git commands (`terminal`). Never assign execution of sync scripts or full-snapshot tasks to profiles with `forbidden_toolsets: [terminal, code_execution]` (e.g. `NEXUS`). When a task scope is superseded to require shell execution, fail-closed as capability-blocked and route execution to an authorized Git steward (`ATLAS` or operator); do not permit restricted workers to substitute ad-hoc partial file copies using file tools.
     - *Worktree Cleanliness & Two-Phase Sealing:* `sync.sh --apply` strictly requires a clean git worktree (`Refusing --apply: Git worktree is not clean`). When uncommitted candidate changes exist in the repo worktree (e.g. from prior curator consolidations or partial syncs), workers and orchestrators must never bypass the guard via `git reset`, `git checkout .`, `git stash`, or partial file copies. Execute a strict two-phase seal:
       1. *Phase 1 (Candidate Baseline):* Run pre-seal validations (`python3 scripts/validate_fleet.py --repo-root .`, `git diff --check`, focused secret scan), verify empty cached index, stage all intended candidate changes (`git add -A`), commit with a descriptive baseline message, and push to origin.
       2. *Phase 2 (Point-in-Time Snapshot):* From the now-clean baseline, run `HERMES_HOME=/root/.hermes bash scripts/sync.sh --apply` exactly once, record capture timestamp and fingerprints, re-validate, commit the snapshot seal, and push.
     - *Anti-Substitution Invariant (Snapshot vs Narrow Static Parity):* Never substitute partial file copies for a mandated full point-in-time fleet snapshot. A partial copy satisfies neither the sync contract nor full-capture evidence. If the mission requirement formally narrows to static parity for specific files, commission a distinct narrow-parity card with explicit file boundaries and sanitation rules; never misrepresent partial file copies as a completed full snapshot.
6. **Fleet Policy Validation & Clean Diff Gates:**
   - Execute `python3 scripts/validate_fleet.py --repo-root /root/projects/hermes-fleet-profiles`.
   - All role toolsets, model parity checks, and SOUL invariants must return `Fleet policy: PASS`.
   - *Pre-Commit Whitespace & Formatting Gate (`git diff --check` & `git diff --cached --check`):* Always run `git diff --check` on tracked modifications, followed by `git diff --cached --check` immediately after staging (`git add -A`). Crucial distinction: `git diff --check` on a dirty tree completely ignores untracked files, leaving trailing whitespace or EOF blank lines in newly created docs undetected until staged. If `git diff --cached --check` catches whitespace errors in a staged candidate:
     1. *Zero-Discard Invariant:* Do NOT run `git reset`, `git checkout .`, `git clean`, or `git stash`—doing so wipes out pre-staged candidate progress.
     2. *No Blind Sync Re-Run:* Do NOT attempt `sync.sh --apply` to fix it; sync scripts fail-closed on dirty/staged worktrees.
     3. *Surgical In-Flight Remediation:* Fix the whitespace defect in the authoritative live file under `/root/.hermes/profiles/` first. Then copy ONLY that single repaired file into its matching repository path, re-stage it (`git add <path>`), and verify `git diff --cached --check` returns clean (0 errors).
     4. *Proceed with Two-Phase Baseline:* Commit and push the clean staged baseline first; only then execute the final authorized point-in-time sync capture.
   - *Secret Pattern Scan Scoping & Documentation False Positives:* Run regex checks over git additions (`git diff -U0 ... | grep '^\+'`) for credential tokens (`ghp_`, `sk-`, `PMAK-`, private keys) to ensure sanitizers operated fail-closed before staging. Do not run broad context-inclusive diff greps over markdown documentation discussing token formats or diagnostic file references (e.g. `delegate-task-concurrency-diagnosis.md`), which cause false-positive secret alarms.
   - *Tracked Parity Verification & Post-Sync Drift Resolution:* Compare tracked repository files against sanitized live profile contents (`tracked_declarative_parity`) to ensure 0 parity mismatches. Because active profile gateways continue processing tasks and updating telemetry or skills mid-flight, verify whether drift occurred between initial sync and seal. If declarative files drifted, sync the exact drifted files into the worktree, verify byte identity (`cmp`) and `git diff --check`, and ensure all tracked declarative files match live disk before committing.
   - *Post-Apply Live Drift & Constrained Single-File Reconciliation Pattern:*
     When an active profile or concurrent turn introduces a declarative edit (e.g. skill addition) immediately after `sync.sh --apply` has already staged the snapshot:
     1. *Zero-Second-Apply Invariant:* Never run a second `sync.sh --apply`—it fails closed on dirty/staged worktrees (`Refusing --apply: Git worktree is not clean`) and produces redundant snapshot directories.
     2. *Zero-Discard Invariant:* Never discard, stash, or reset staged work (`git reset`, `git checkout .`, `git stash`).
     3. *Surgical File Copy:* Authorize a constrained single-file reconciliation by copying ONLY the specific drifted declarative file(s) from live disk into their matching repository path.
     4. *Re-run Pre-Commit Gates:* Run `python3 scripts/validate_fleet.py`, unit tests, `git diff --cached --check`, and a narrow added-lines secret scan covering the newly added delta.
     5. *Unified Commit Seal:* Stage the reconciled file and commit the combined snapshot plus delta together, then push to origin and verify `HEAD == origin/main`.
   - *Point-in-Time Snapshot Certification vs Live-Drift Review Loop Invariant:* In an active multi-agent fleet, both runtime telemetry (`.usage.json`, curator state) and declarative files (skills, configs, documentation) continuously evolve as agents execute concurrent tasks. Requiring real-time byte-for-byte parity (`cmp`) against live disk across multi-turn asynchronous review cycles creates an infinite stale-drift rejection loop where each review pass discovers newer drift. Enforce strict **point-in-time capture semantics**:
     1. The worker performs ONE capture run (`sync.sh --apply` or single-pass copy) and immediately records the UTC capture timestamp and SHA-256 fingerprints of critical configs, representative skills, and telemetry files.
     2. Reviewers and verifiers must evaluate and certify the repository candidate strictly as of that recorded capture point-in-time (validating internal consistency, policy compliance via `validate_fleet.py`, formatting via `git diff --check`, secret sanitization, and matching capture fingerprints).
     3. Reviewers are strictly forbidden from rejecting or blocking a task solely because live disk files changed after the recorded capture timestamp.
     4. *Telemetry File Exemption from Live Equality:* Reviewers must NEVER execute `cmp` against live `.usage.json` or dynamic state files during review. Because reviewer turns generate LLM calls and tool executions, live telemetry files update continuously; the snapshot `.usage.json` is validated solely for JSON schema validity, line-count consistency, and byte-matching the recorded capture hash.
   - *Live-Authoritative Policy Reconciliation Gate:* When syncing a fleet backup where the live fleet is designated the authoritative source of truth, repository validation (`validate_fleet.py`) may fail if live configurations contain toolsets or capabilities not yet enumerated in `governance/roles.yaml` (e.g. `ERROR: <profile>: platform tools outside policy: [...]`). In this state, do NOT alter or sanitize away the live configuration to appease the validator. Reconcile the policy manifest (`governance/roles.yaml`) by adding the live-authorized capability to the specific scope (`profiles.<name>.allowed_platform_toolsets` vs `profiles.<name>.allowed_root_toolsets`), re-render via `python3 scripts/apply_role_policy.py`, and re-run validation until `Fleet policy: PASS`. Policy must codify verified live architecture, not truncate it. Platform tools (like `connections` used in A2A or messaging gateways) must never be added to root toolsets.
7. **Git Commit & Remote Push:**
   - Check `git status`, add all synced declarative updates, author a descriptive commit, and push upstream to `origin main`.
   - Ensure user project working branches (e.g. `digiflazz-topup-bot`) are also pushed to their respective remotes.

## 9. Full-Stack Fleet Bootstrapping & Baseline Ratification Pipeline
When bootstrapping or inheriting a software engineering mission from a formal PRD or Agent-Ready Execution Pack:
1. **Workspace & Baseline Ratification:**
   - *Greenfield Init:* For new projects, initialize `/root/projects/<app_name>` git repository and commit the ratified PRD to `docs/PRD.md` as the inviolable baseline before authoring code.
   - *Unsealed/Brownfield Baseline Guard:* When inheriting an existing repository containing uncommitted modifications (`M`) or untracked files (`??`):
     - *Zero-Reset Invariant:* Never run `git reset`, `git checkout .`, `git clean`, or `git stash` to force a clean tree. That destroys prior developer/agent work and wipes scaffolding.
     - *Uncertified Baseline Segregation:* Explicitly declare current HEAD + dirty tree as an unsealed baseline (`UNVERIFIED / NOT RELEASE CANDATE`). No release candidate can be certified until a clean, audited commit SHA is sealed.
     - *Read-Only Baseline Audit (WP00):* Dispatch read-only audit lanes (FORGE for backend/schema, SENTINEL for security/privileges, NEXUS for traceability) before permitting code writes or migrations.
     - *Contract Gap Mapping:* Require FORGE to audit existing code against ratified domain contracts (locking order, stock algebra, idempotency, wallet headroom, outbox) and produce an explicit contract-gap ledger before authoring new migrations or modifying shared state.
     - *Immediate 'DO NOT RELEASE' on Unauthenticated Financial Findings:* When an independent security baseline (SENTINEL) discovers unauthenticated money settlement (e.g. `simulate=paid` GET endpoints), unauthenticated admin balance credit, balance-cap bypasses, or BOLA on invoices/attachments, ORION must immediately declare an authoritative `DO NOT RELEASE / DO NOT ENABLE MONEY FLOWS` freeze. UI integration (`FRAME`) and deployment (`ATLAS`) must not proceed against unauthenticated routes. The orchestrator must sequence a strict single-writer remediation lane for `FORGE` to freeze server-side authorization and database-level financial constraints, require a clean sealed commit SHA, and gate release on fresh same-revision retests by `PRISM` and `SENTINEL`.
2. **Agent-Ready Execution Pack Intake & Authority Hierarchy:**
   - *Manifest Verification Gate:* When an execution pack is delivered as a bundle, verify integrity (`sha256sum -c MANIFEST.sha256`) before task dispatch.
   - *Normative Authority Precedence:* Resolve document conflicts strictly by hierarchy:
     1. Actual legal / regulatory / provider obligations and fleet governance (inviolable).
     2. Valid Human Owner DecisionRecords (explicit commercial/scope decisions).
     3. Decision register and launch gates (`07_DECISIONS_AND_LAUNCH_GATES.md`).
     4. Domain correctness contracts and provider integration contracts (`03_DOMAIN_CONTRACTS.md`, `04_PAKASIR_INTEGRATION_CONTRACT.md`).
     5. PRD product/UX specification (`01_PRD`).
     6. Acceptance test catalog and traceability matrix (`06`, `09`).
     7. Historical reference / v1 draft (intent context only; never overrides approved v2 rules).
   - *Unresolved External Provider & Mock-First Boundary:* When third-party provider APIs (payment gateways, external auth) lack official account-specific verification, classify the integration as `UNRESOLVED` (`PAYMENTS_LIVE_ENABLED=false`). Never invent HMAC headers, request signatures, or fake transaction IDs. Implement a provider-neutral mock adapter first so domain logic, test suites, and UI can progress locally while live launch gates (G-01..G-08) await owner credentials and authorization.
3. **Canonical 6-Layer Dependency DAG:**
   - **Layer 1 (Parallel Architecture & Tokens, Priority 100):**
     - `AURORA`: Visual Design System, Design Tokens, Editorial Style, Anti-AI Slop UI contract (`docs/DESIGN_SYSTEM.md`).
     - `FORGE`: PostgreSQL schema (Drizzle ORM), atomic inventory reservation, immutable wallet ledger, payment gateway client & background worker sweeps.
   - **Layer 2 (Frontend Implementation, Priority 90):**
     - `FRAME`: Storefront, bottom-sheet cart, dynamic input view, QRIS payment screen with 1-tap save, customer dashboard, admin portal. Parents: `AURORA` + `FORGE`.
   - **Layer 3 (Runtime & Infrastructure, Priority 80):**
     - `ATLAS`: DB provisioning, background worker daemon (systemd), ingress reverse proxy (Nginx / Cloudflare Tunnel) with SSL/TLS, health check. Parent: `FRAME`.
   - **Layer 4 (Independent Dual-QA Gate, Priority 70):**
     - `PRISM`: Concurrency race testing (anti-overselling), webhook idempotency, late-payment ledger reconciliation, wallet boundary checks. Parent: `ATLAS`.
     - `LENS`: Rendered visual QA across 360px/390px/412px viewports, anti-slop compliance, 0px horizontal overflow, touch target geometry (>=44px). Parent: `ATLAS`.
   - **Layer 5 (Security Audit, Priority 65):**
     - `SENTINEL`: RBAC boundary verification (Owner vs Admin/Support financial mutations), IDOR on invoice attachments, secret sanitization. Parents: `PRISM` + `LENS`.
   - **Layer 6 (Production Release & Synthesis, Priority 50):**
     - `ORION`: Verification synthesis, Definition of Done compliance audit, operational handoff. Parent: `SENTINEL`.
4. **Concurrency Sequence Invariant (Postgres Advisory Locks):**
   - In high-concurrency checkout races, autoincrement sequence counters or non-locked queries for human-readable order/invoice numbers (e.g. `ORD-YYYYMMDD-XXXX`) collide or abort under row contention.
   - Serialize sequence generation inside transactions using PostgreSQL transaction-scoped advisory locks (`SELECT pg_advisory_xact_lock(hashtext('order_seq_lock'))`) so concurrent checkouts serialize cleanly without deadlocks or sequence gaps.
5. **Defect Disposition Ledger & Conditional Staged Release:**
   - When independent verifiers (SENTINEL, LENS) surface material P0/P1 defects (e.g. missing session auth on admin APIs, attachment BOLA/IDOR, WCAG contrast violations) alongside verified core business logic:
     - Do not declare unconditional production release.
     - Do not halt the entire project if public read-only discovery is functional and secure.
     - Author an explicit **Defect Disposition Ledger** mapping each defect to its owning specialist (`FORGE` for security/API, `FRAME` for UI/WCAG), severity (`P0/P1/P2`), exact reproduction tests, and target files.
     - Issue a **Conditional Staged Release (Restricted Beta)**: public catalog/browsing approved on live ingress, while checkout, payment processing, and admin mutations remain gated until the remediation sprint completes.
