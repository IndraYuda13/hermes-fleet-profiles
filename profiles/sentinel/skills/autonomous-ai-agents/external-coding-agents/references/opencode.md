# OpenCode quick reference

Use `opencode run '...'` for bounded, non-interactive work. Confirm the resolved binary and authentication when its behavior differs across shells. Interactive OpenCode requires a PTY-backed background process; monitor its logs and exit with Ctrl+C rather than `/exit`. Keep concurrent sessions in separate worktrees and independently review all resulting changes.
